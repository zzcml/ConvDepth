(networks/ConvNext_encorder_v2.py and networks/Convnext_decoder.py are both Python source files, so no files in the change list match the criteria for being added to .gitignore)

# No changes needed - only Python source files were modified